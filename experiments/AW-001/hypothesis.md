# AW-001 hypothesis

Selected AI user-agents (or `Accept: text/markdown` clients) may receive a markdown twin while ordinary browsers receive HTML for the same URL path.

We measure exposure differences; we do not claim universal SEO lift from Markdown.
