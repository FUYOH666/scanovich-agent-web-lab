# Public sample intents (v0.1)

20 prompts for reproducibility demos. **No gold labels.**

Holdouts (60) and adversarial (20) live in the sealed vault; only their SHA-256 is public under `preregistration/holdout-manifest.sha256`.
