# Basket: synthetic

Scripted harness / API / curl probes initiated by lab operators.

- Public reproducibility samples: `public/intents_v0.1.jsonl` (20 prompts, no gold).
- Holdouts and gold labels: **not here** — see `docs/sealed-vault.md`.

When writing observations, set `"origin": "synthetic"`.
